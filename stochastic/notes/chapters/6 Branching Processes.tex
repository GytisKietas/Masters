\section{Branching Processes}

Assume a random variable $X_n$ describes the total population size  after $n$ generations. Each individual in generation $n$ gives birth to $Y_n$ offspring, independently from other individuals. ${\rm Pr} \{ Y_n = k \} = p_k$, here $\sum_{k=0}^\infty = 1$. If, in some generation $X=0$, then the banching process stops.

For the random variable $Y$: $G_{Y}(s) = \sum_{k=0}^{\infty} p_k s^k$, where $p_k = {\rm Pr \{Y_n = k\}}$. Assume that the initial population size was $X_0=1$. Then for the first generation, the random variable $X_1$ will be just equal to $Y$: $X_1 = Y$l

Note that for a branching process extinction probability condition (or condition of stationarity) is $g(s) = s$, where 